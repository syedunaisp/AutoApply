{
  "name": "@autoapply/db",
  "version": "1.0.0",
  "private": true,
  "main": "./schema.ts",
  "types": "./schema.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "build": "echo 'db package — no build step'"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.5.0"
  }
}
